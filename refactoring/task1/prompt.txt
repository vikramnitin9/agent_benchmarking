Refactor the code in `refactoring/task1/<dirname>` by separating each class into a different file.
Create all the necessary header files.
Also modify CMakeLists.txt accordingly, and make sure that the project still compiles.